# PRD WEBSITE PORTOFOLIO
## SPESIFICATION
-   Static Website.
-   HTML 5.
-   Tailwind css.
-   Vanilla Javascript.
-   Must support three devices (desktop, mobile, and tablet).
-   For image, use the placeholder first for implementation.

## TARGET DEPLOYMENT
-   github with domain github.io
-   url must be: robbyaliasaakbar.github.io

## PURPOSE OF CREATING THE WEBSITE
-   To professionally showcase to the public all the knowledge I possess in the domain of artificial intelligence.

## TARGET AUDIENCE
-   HRD (Recruiter)
-   Bussiness Owner
-   Tech Investor

## PAGE STRUCTURE
public_html/
├── index.html                          ✅ Homepage
├── profile.html                        ✅ Profile Page
├── experiments-list.html               ✅ Experiments List page
│   └── exp001.html                     ✅ Page experiments 001 page
│   └── exp002.html                     ✅ Page experiments 002 page
│   └── exp003.html                     ✅ Page experiments 003 page
│   └── exp004.html                     ✅ Page experiments 004 page
├── contact.html                        ✅ Contact Page

## STYLE WEBSITE
-   It MUST look professional, minimalist, interactive, and modern.
-   Use a font that doesn't look overly "tech." Instead, opt for one that looks clean, minimalist, and professional.
-   Primary Collor Pallete:
    1.  White: #FFFFFF
    2.  Black: #0C0C0C
    3.  Red:   #D70000

## TEMPLATE NAVBAR & FOOTER
### NAVBAR
-   Left Section: Logo Text (ROBBY ALIASA AKBAR)
-   Right Section: Navbar Direct To Page (With Hover, and Active Page)
    Home - Profile - Experiments - Contact

### FOOTER
#### LEFT SECTION
-   Logo Text (Robby Aliasa Akbar)
-   Tagline (AI SYSTEMS BUILDER & EXPERIMENTER)
-   Sub Tagline (Exploring AI systems, local LLMs, automation workflows, and practical tools that actually work in real business operations.
Building what matters, and checking whether AI is truly useful in everyday work.)

#### MIDDLE SECTION
-   Navbar Direct To Page (With Hover, and Active Page)
    └── Home
    └── Profile
    └── Experiments
    └── Contact

#### RIGHT SECTION
SOCIAL MEDIA LINK:
    └── Title (Linkedin), Link: www.linkedin.com/in/ibbor-arts-001a2a267
    └── Title (Reddit), Link: https://www.reddit.com/user/Full_Director87/
    └── Title (Instagram), Link: https://www.instagram.com/ybboraa/
    └── Title (Email), Direct Email to: ibborarts@gmail.com
    └── Title (Whatsapp), Direct whatsapp to: wa.me/+6282249072873
---

---
# HOMEPAGE CONTENT SPESIFICATIONS
## HERO SECTION
-   Full Name: Robby Aliasa Akbar
-   Tagline: AI SYSTEMS BUILDER & EXPERIMENTER
-   Sub Tagline 1: Exploring AI systems, local LLMs, automation workflows, and practical tools that actually work in real business operations.
Building what matters, and checking whether AI is truly useful in everyday work.
-   Sub Tagline 2 (Blockquote): Local LLMs, API llms, Cloud LLMs, and Manymore.
-   Sub Tagline 3 First Paragraph: I explore, build, test, and optimize AI systems, automation workflows, and LLM experiments to find what works in real conditions. 
-   Sub Tagline 3 Second Paragraph: I also implement business automation leveraging AI and n8n workflows to streamline customer service and operational administration.
-   CTA link Direct to page: Experiment List page
-   CTA link Direct to page: Contact page

## "SELECTED EXPERIMENTS" SECTION
### Exp 001
-   Title: The 35B MoE model runs on an older architecture with 12GB of VRAM.
-   Description: Testing whether 12GB of VRAM and an older GPU architecture can run open-source LLMs optimally without putting undue strain on the GPU.
-   Direct link to page: Page experiments 001 page
-   There must be a 'stable' status indicator.

### Exp 003
-   Title: Automation agent workflow for business research needs.
-   Description: What does it actually take to optimally use AI as an autonomous research assistant? Is it just the model? As it turns out, no.
-   Direct link to page: Page experiments 003 page
-   There must be a 'stable' status indicator.

### Exp 004
-   Title: Creating digital employees with the help of open-source LLMs.
-   Description: Is it possible to set up customer service, operational admin, ads specialist, data analyst, and finance functions using only workflows and open-source LLMs, given adequate hardware and reasonable resource usage? 
-   Direct link to page: Page experiments 004 page
-   There must be a 'stable' status indicator.

### Direc link
-   Title: View Experiments List ->
-   Link: Experiments List page

## "CAPABILITY" SECTION
1.  - Title: BUSINESS-ORIENTED APPLICATION
    - Description: Directing AI capabilities toward real problems, practical workflows, and measurable value.
2.  - Title: TECHNICAL PROBLEM SOLVING
    - Description: Investigating bottlenecks, testing variables, and iterating based on actual behavior.
3.  - Title: AI SYSTEMS BUILDING
    - Description: Connecting model, infrastructure, tools, data, memory, and automation into usable systems. 
4.  - Title: AI EXPLORATION
    - Description: Exploring emerging AI capabilities and identifying practical limits.

## "APPROACH" SECTION
-   Evidence-driven
-   Hardware-agnostic
-   Experiment-first
-   System-level thinking
-   Practical usability
-   Closing Blockquote: I do not claim to know every answer in AI. I build, test, measure, investigate failure, and find what actually works.

## "CLOSING" SECTION
-   Title: ADAPTIVE BY DESIGN
-   Description:    AI systems are evaluated as complete systems: hardware, OS, runtime, inference engine, model, context, and application.
                    The goal is not to limit experimentation to one platform, but to understand how AI systems behave across different environments.

## "SPOILER" PROFILE PAGE
-   Title: ABOUT
-   Description: I'm a self-taught AI systems builder and experimenter. I explore local LLMs, inference, automation, agent architecture, and practical AI systems with a focus on finding what actually works.
-   Direct Link Profile Page with title (Read Full Profile ->)

## "SPOILER" CONTACT PAGE
-   Header: Contact
-   Title: Have a problem worth exploring?
    Description: If you have a business problem, technical question, or AI opportunity that needs validation, feel free to reach out.
-   CTA link direct to whatsapp: wa.me/+6282249072873

---

---
# PROFILE PAGE CONTENT SPECIFICATIONS

## HERO SECTION (IDENTITY)
-   Eyebrow Label: IDENTITY
-   Full Name: Robby Aliasa Akbar
-   Tagline: AI Systems Builder & Experimenter
-   Sub Tagline 1: Exploring what AI can do.
Building what works, and are they REALLY useful for everyday life?
-   Sub Tagline 2 (Blockquote): Local LLMs, API llms, Cloud LLMs, and Manymore.
-   Sub Tagline 3 First Paragraph: I also implement business automation leveraging AI and n8n workflows to streamline customer service and operational administration.
-   Sub Tagline 3 Second Paragraph (Blockquote): I'm a self-taught AI systems builder and experimenter. I explore local LLMs, inference, automation, agent architecture, and practical AI systems with a focus on finding what actually works.

## POSITIONING SECTION
-   Eyebrow Label: POSITIONING
-   Paragraph 1: I'm an independent AI Systems Builder and Experimenter.
-   Paragraph 2: I'm not here to claim mastery over every AI technology. I'm here to explore what emerging AI capabilities can actually do, test them under real-world conditions, and build systems that genuinely work.
-   Blockquote: Exploring what AI can do. Building what works. And asking one more question: is it REALLY useful in everyday life?

## JOURNEY SECTION
-   Eyebrow Label: JOURNEY
-   Format per item: Step Number — Stage Title, Phase Label (small tag), Description

### 01 — AI USER
-   Phase Label: STARTING POINT
-   Description: Starting as a regular AI user without deep technical understanding. Using AI tools to solve everyday tasks, without understanding the architecture behind them.

### 02 — AI EXPLORER
-   Phase Label: DEEPENING
-   Description: Moving from using AI to understanding how AI systems work. Exploring local LLMs, model architecture, quantization, inference engines, and hardware requirements.

### 03 — AI BUILDER
-   Phase Label: CREATING
-   Description: Building integrated systems using models, tools, automation, memory, and infrastructure. Moving from running models to orchestrating complete AI workflows.

### 04 — AI SYSTEMS EXPERIMENTER
-   Phase Label: CURRENT
-   Description: Testing limits, measuring behavior, investigating bottlenecks, and refining systems based on evidence. Focused on understanding what works in real conditions.

## FOCUS AREAS SECTION
-   Eyebrow Label: FOCUS AREAS

### LOCAL AI & INFERENCE
-   Description: Model deployment, quantization, runtime behavior, CPU/GPU configuration, performance measurement.

### AI SYSTEMS & INFRASTRUCTURE
-   Description: Integration between model, interface, automation, tools, memory, and workflow.

### AGENT ARCHITECTURE
-   Description: Systems that can accept goals, use tools, retrieve information, reason, act, and iterate.

### HARDWARE-AWARE AI EVALUATION
-   Description: Understanding AI systems as a full stack: hardware, OS, runtime, engine, model, context, and application.

## HARDWARE-AGNOSTIC APPROACH SECTION
-   Eyebrow Label: HARDWARE-AGNOSTIC APPROACH
-   Paragraph 1: I approach AI systems with a strong focus on adaptability across different hardware environments.
-   Paragraph 2: I'm not tied to a specific GPU vendor, device class, or platform. Instead, I focus on understanding how the entire system behaves — from hardware and drivers to inference engines, models, and workloads — and finding ways to make AI work within the constraints of what's actually available.
-   Intro line: This includes evaluating:
-   Checklist (8 items):
    1.  CPU and GPU behavior
    2.  Memory and bandwidth limits
    3.  Runtime compatibility
    4.  Inference configuration
    5.  Model architecture
    6.  Context length
    7.  Thermal and power behavior
    8.  Stability and usability

## PRINCIPLES SECTION
-   Eyebrow Label: PRINCIPLES

### EVIDENCE IS EVIDENCE, NOT A CONCLUSION
-   Description: Observations must be understood in context. Experimental results are separated into: observation, interpretation, hypothesis, and conclusion.

### NEWBIE MINDSET
-   Description: Assumptions are treated as hypotheses. When evidence contradicts understanding, the mental model changes.

### AI AS A BUILDING PARTNER
-   Description: AI is used as a partner for exploration, debugging, coding, research, and documentation. But AI output is still tested against documentation, experiments, and actual system behavior.

### CODING AS A TOOL
-   Description: Coding is used to understand, modify, automate, and control AI systems, not as the final goal.

## VALUE PROPOSITION SECTION
-   Eyebrow Label: VALUE PROPOSITION

### AI FEASIBILITY ASSESSMENT
-   Description: Evaluating whether an AI idea is technically realistic and how to approach it.

### AI SYSTEM ARCHITECTURE
-   Description: Designing end-to-end AI systems based on real requirements.

### HARDWARE-AWARE OPTIMIZATION
-   Description: Choosing model, quantization, runtime, and inference configuration based on environment constraints.

### AI WORKFLOW & AGENT DEVELOPMENT
-   Description: Connecting LLMs with tools, knowledge, memory, automation, and external services.

### TECHNICAL EXPERIMENTATION
-   Description: Testing capabilities, limitations, and trade-offs before committing to larger implementation.

## CLOSING SECTION
-   Closing Statement: I don't claim to have all the answers in AI.
-   Closing Paragraph: What I do believe in is my ability to find them by asking the right questions, experimenting, testing assumptions, learning from failures, and validating what actually works. When something works, I turn it into a practical system.
-   CTA link Direct to page: Experiment List page (Label: VIEW EXPERIMENTS)

---

---
# EXPERIMENTS LIST PAGE CONTENT SPECIFICATIONS

## HERO SECTION
-   Eyebrow Label: EXPERIMENTS
-   Title: A collection of experiments.
-   Sub Tagline (Blockquote): Exploring AI capabilities, limitations, and practical usability. Each experiment is documented as a process: problem, hypothesis, experiment, failure, iteration, evidence, result, and lesson.

## STATUS LEGEND SECTION
-   Eyebrow Label: STATUS
-   Must display all 4 possible status states as a legend/key (even though only "STABLE" is currently used across existing experiment items — the other 3 states must still exist visually as component variants for future experiments):
    1.  WORKING ON IT
    2.  EXPLORING
    3.  FAILED
    4.  STABLE

## EXPERIMENT GRID SECTION
-   Eyebrow Label: SELECTED EXPERIMENTS
-   Grid must show all 4 experiment items (unlike Homepage, which only teases 3):

### Exp 001
-   Title: The 35B MoE model runs on an older architecture with 12GB of VRAM.
-   Description: Testing whether 12GB of VRAM and an older GPU architecture can run open-source LLMs optimally without putting undue strain on the GPU.
-   Status: STABLE
-   Direct link to page: exp001.html

### Exp 002
-   Title: Prefill speed on long context inference.
-   Description: How can open-source LLMs achieve acceptable and reasonable prompt processing (prefill) speeds when the total context reaches 250K+ tokens?
-   Status: STABLE
-   Direct link to page: exp002.html

### Exp 003
-   Title: Automation agent workflow for business research needs.
-   Description: What does it actually take to optimally use AI as an autonomous research assistant? Is it just the model? As it turns out, no.
-   Status: STABLE
-   Direct link to page: exp003.html

### Exp 004
-   Title: Creating digital employees with the help of open-source LLMs.
-   Description: Is it possible to set up customer service, operational admin, ads specialist, data analyst, and finance functions using only workflows and open-source LLMs, given adequate hardware and reasonable resource usage?
-   Status: STABLE
-   Direct link to page: exp004.html

## CLOSING SECTION
-   Closing Line: Each experiment is documented as a technical case study, showing not just results, but the process of thinking, testing, failing, and iterating.
-   CTA link Direct to page: Contact page (Label: GET IN TOUCH)

---

---
# EXPERIMENT DETAIL PAGE TEMPLATE (exp001.html–exp004.html)

## TEMPLATE STRUCTURE & FLEXIBILITY RULES
This page type is a **reusable dynamic template**, not a fixed-field page — content blocks below must be built as flexible/optional components since the 4 existing experiments do not all use the same fields. Rules the redesign must respect:

1.  **Header block (fixed for every experiment):**
    -   Eyebrow Label: "EXPERIMENT 00X"
    -   Title (H1): experiment title
    -   Sub Tagline: 1–2 sentence experiment question/description
    -   Status Badge: one of WORKING ON IT / EXPLORING / FAILED / STABLE (position: top of page, near header)

2.  **System Requirements block (dynamic key–value grid):**
    -   Field COUNT and field NAMES vary per experiment (ranges from 8 to 10 fields; e.g. exp001/002 use CPU/RAM/GPU/Backend/OS/Inference Engine/Frontend UI/Model/Context/Status; exp003 swaps in MCP Tools/Jinja/System Instruction instead of CPU/RAM/GPU; exp004 drops "Context" and adds "Model For Parsing").
    -   Layout must be a flexible grid/table component that can render any number of label+value pairs, not a hardcoded fixed set of fields.

3.  **Screenshot block (dynamic image gallery):**
    -   Image COUNT varies per experiment: exp001/002/003 = 1 screenshot, exp004 = 3 screenshots.
    -   Must support a gallery/carousel-capable layout for 1–N images (use placeholder images per existing spec).

4.  **Visualization block (dynamic chart/visual type):**
    -   Type varies per experiment — must support at least 3 visual variants:
        a.  **Line/Bar chart** (exp001 uses two: a bar chart for decode speed vs. `--n-cpu-moe` value, and a line chart for prompt processing speed vs. stage context; exp002 uses one line chart for prefill speed vs. context length).
        b.  **Percentage/success-rate chart with abbreviation glossary** (exp003: a step chart showing output quality % across 6 cumulative optimization stages, plus a glossary explaining each abbreviation used in the chart: MCP, RAG, KVCM, LP, DSP, FTD).
        c.  **Before/after comparison visual** (exp004: "AI-First vs Deterministic-First" comparison, described as a 0%→100% / red→green gradient scale comparing two labeled methodologies rather than a data chart).
    -   Each visualization has its own title + 1-sentence caption above it.

5.  **Experiment Details narrative block (semi-fixed subsection order):**
    Standard order: PROBLEM → HYPOTHESIS → EXPERIMENT → WHAT FAILED (label varies: "WHAT FAILED" or "FAILURE AND NEW ITERATION") → ITERATION → EVIDENCE → RESULT → WHAT I LEARNED → PRACTICAL IMPLICATION.
    -   Exception: exp004 inserts an extra **SPECIFICATIONS** subsection (a numbered list of 8 system requirements) between the intro description and HYPOTHESIS — template must allow optional extra subsections between PROBLEM and HYPOTHESIS.
    -   **EVIDENCE** subsection contains a variable-count grid of small stat/metric tiles (label + value), ranging from 5 tiles (exp003) to 8 tiles (exp001) — must be a flexible stat-grid component, not fixed at N tiles.

6.  **Configuration Template block (OPTIONAL — only present on exp001 & exp002):**
    -   A labeled code block (path label above it, e.g. "~/Path build llama.cpp") containing a shell/config snippet.
    -   Followed by a numbered explanatory list (blockquote style) walking through key flag choices.
    -   Not present on exp003/exp004 (workflow/automation experiments have no CLI config) — template must render this block conditionally.

7.  **Non-technical summary block (fixed for every experiment):**
    -   A visually distinct callout/quote block titled: "FOR THOSE OF YOU UNABLE TO READ THIS DATA FROM TECHNICAL STANDPOINT, HERE IS THE CONCLUSION"
    -   Followed by 3–6 short paragraphs restating the experiment in plain language.

8.  **Footer CTA block (fixed for every experiment):**
    -   Link back: "Back to Experiments" → Experiments List page
    -   Link forward: "Have a problem worth exploring?" → Contact page

---

## EXPERIMENT 001 CONTENT — "The 35B MoE model runs on an older architecture with 12GB of VRAM."

**Sub Tagline:** Testing whether 12GB of VRAM and an older GPU architecture can run open-source LLMs optimally without putting undue strain on the GPU.

**Status Badge:** STABLE

**System Requirements:**
-   CPU: Intel Core i5 11400f
-   RAM: 16GB DDR4 3200 MT/s
-   GPU: Radeon RX 6700 XT
-   Backend: ROCm Native
-   OS: Ubuntu 26.04 LTS
-   Inference Engine: LLaMa.cpp upstream
-   Frontend UI: OpenWebUI v0.11.0
-   Model: Ornith-1.0-35B-A3B-IQ4_NL-GGUF
-   Context: 128K
-   Status: Stable

**Screenshot:** 1 image (experiment001.webp)

**Visualization 1 — "Decode Speed With --n-cpu-moe Value Configuration":**
-   Caption: Measured tokens/second across different `--n-cpu-moe` flags.
-   Chart type: Bar chart
-   Range label: Low (18 tok/s) → High (28 tok/s)
-   Data (n-cpu-moe value → tok/s): 18→28, 20→25, 22→23(approx), 24→22, 25→20, 26→18 *(axis values as published: y-axis 3020100 / labels 2224262830 tok/s; x-axis values 2825232018)*

**Visualization 2 — "Prompt Processing Speed With Total Tokens Processing":**
-   Caption: Measured tokens/second across different stage Processing tokens
-   Chart type: Line chart
-   Meta label: Total Processing Tokens: 27K
-   Range label: Low (488 tok/s) → High (582 tok/s)
-   Data (stage context → tok/s): 19.7K→582, 20.8K→548, 21.6K→531, 22.8K→509, 23.8K→502, 24.9K→497, 25.9K→492, 26.9K→488

**Experiment Details:**
-   PROBLEM: Can 35B MoE model run practically on 12GB VRAM GPU? The goal is to find the optimal configuration that allows for stable performance without exceeding hardware limitations.
-   HYPOTHESIS: MoE architecture may provide better usability than dense models because active parameters are smaller, even if total model size is large.
-   EXPERIMENT: Testing the model layer placement configuration using the expert splitting method on the CPU.
-   WHAT FAILED: Several configurations hit memory limits, bandwidth bottlenecks, or unstable thermal behavior.
-   ITERATION: The backend adjustment (ROCm) for AMD must be native. Otherwise, prefill speeds are unacceptable. This involves optimizing KV cache management and adjusting the scheduler. But need GPU optimization too for better performance.
-   EVIDENCE (8 stat tiles):
    -   DECODE: up to 28 tok/s
    -   CONTEXT: 128K
    -   PREFILL: up to 581 tok/s
    -   MEMORY PRESSURE: Manageable
    -   OUT OF MEMORY: Never
    -   USER EXPERIENCE: losless
    -   JUNCTION GPU THERMAL: Max 85
    -   EDGE GPU THERMAL: Max 60
    -   (additional tile) VRAM GPU THERMAL: Max 70
    -   (additional tile) STABILITY: Stable
-   RESULT: The model achieved stable performance with acceptable latency, proving that constrained hardware can handle large MoE models with proper optimization.
-   WHAT I LEARNED: MoE models can be viable on constrained hardware when quantization and memory management are properly tuned. The key is finding the right balance between model size, quantization level, and context length.
-   PRACTICAL IMPLICATION: This approach can inform deployment decisions on constrained environments, showing that large models don't always require expensive hardware if properly optimized.

**Configuration Template:**
-   Label: ~/Path build llama.cpp
```
MODEL="path model" (model 35B A3B) \
  --host 0.0.0.0 \
  --port 8080 \
  --n-gpu-layers 99 \
  --threads 4 \
  --threads-batch 4 \
  --n-cpu-moe 26 \
  --moe-cache auto \
  --ctx-size 131072 \
  --batch-size 1024 \
  --ubatch-size 512 \
  --keep 8000 \
  --cache-type-k f16 \
  --cache-type-v f16 \
  --swa-checkpoints 16 \
  --checkpoint-min-step 2048 \
  --embd-normalize 0 \
  --no-kv-unified \
  --kv-offload \
  --jinja \
  --flash-attn on \
  --parallel 1 \
  --cache-ram 8192 \
  --cache-idle-slots \
  --temp 0.6 \
  --top-k 0 \
  --top-p 0.95 \
  --min-p 0 \
  --repeat-penalty 1 \
  --repeat-last-n 0 \
  --alias coding \
  --log-verbosity 4
```
-   Explanatory note above list: Do not copy this configuration blindly; it must be adjusted to suit the specific characteristics of your model and hardware. This configuration serves only as a reference, as my choices were based on specific reasons that I will explain below.
-   Numbered explanation (8 points):
    1.  I chose `--n-gpu-layers 99` as a baseline to offload the entire model to the GPU.
    2.  I chose `--threads 4` because testing showed this to be my "sweet spot." This setting configures CPU threads to assist with token generation; the optimal value cannot simply be guessed but must be determined through testing based on your CPU's thread count. I happen to use an i5-11400F (6 cores, 12 threads), and 4 threads worked best for me.
    3.  I chose `--threads-batch 4` because my tests showed that higher values increased prefill latency. This happens because too many CPU threads get involved in the prefill process—a task that should ideally rely more on the GPU to maintain reasonable speeds. However, using fewer threads isn't always faster either; bottlenecks can occur because parts of the model's "expert layers" are offloaded to the CPU. Also, monitor the GPU junction temperature: if prefill feels fast but the junction temperature is high, increase this value.
    4.  I chose `--n-cpu-moe 26` to offload 26 experts to the CPU for optimal performance on my hardware. This value isn't universal; it varies depending on the specific model and its quantization.
    5.  Regarding `batch` and `ubatch`: these values shouldn't be chosen arbitrarily, as they affect compatibility with kernels and the inference engine. Higher numbers don't necessarily yield faster results; in fact, they often lead to anomalies—such as unreasonable GPU junction temperatures or even slower prefill speeds.
    6.  I selected `f16` for KV cache compression to maintain memory consistency for the model. Of course, this reduces the number of model layers that can fit into the GPU, but I chose `f16` because, personally, I find a model losing its context far worse than slow inference speeds.
    7.  I chose `--no-kv-unified` and `--parallel 1` because the model is used only by me in a single inference session. If multiple users were accessing the same model running on your PC, you would need to enable `--kv-unified` and increase the `--parallel` value.
    8.  The remaining configurations I haven't covered are standard settings that you can find in any other post.

**Non-technical summary:**
This experiment demonstrated that a 35-billion-parameter AI model can be run practically on a consumer GPU with only 12GB of VRAM.
It wasn't achieved by adding more hardware. It was achieved by understanding the system, identifying its bottlenecks, and carefully balancing what the CPU, GPU, memory, and inference engine were each responsible for.
The important finding isn't that a 35B model can run on a 12GB GPU. It's that hardware limitations can sometimes be worked around through better system understanding and optimization.
The result was a stable configuration that could run the model continuously without running out of memory, while still delivering practical inference performance.
The takeaway: when resources are limited, the answer isn't always to buy more resources. Sometimes, the better answer is to understand what you already have and make it work harder.

---

## EXPERIMENT 002 CONTENT — "Prefill speed on long context inference."

**Sub Tagline:** How can open-source LLMs achieve acceptable and reasonable prompt processing (prefill) speeds when the total context reaches 250K+ tokens?

**Status Badge:** STABLE

**System Requirements:**
-   CPU: Intel Core i5 11400F
-   RAM: 16GBB DDR4 3200 MT/s *(note: verbatim typo "16GBB" on live site)*
-   GPU: RX 6700 XT
-   Backend: ROCm Native
-   OS: Ubuntu 26.04 LTS
-   Inference Engine: llama.cpp
-   Frontend UI: Open WebUI
-   Model: Gemma-4-26B-A4B-it-Q4_K_XL-GGUF
-   Context: 256K
-   Status: Complete *(note: this experiment uses "Complete" instead of "Stable" as the field value, while the badge above still reads STABLE)*

**Screenshot:** 1 image (experiment002.webp)

**Visualization — "Prompt Processing Speed Tok/s vs Context Length":**
-   Caption: Chart of prompt processing speed (prefill) in tok/s, based on total context window length.
-   Chart type: Line chart
-   Data (context length → tok/s): 16K→312, 32K→435, 64K→840, 96K→701 *(published order as-is)*, 128K→635, 192K→542, 256K→272 *(published axis: 16K/32K/64K/96K/128K/192K/256K against values 312/435/840/701/635/542/272)*

**Experiment Details:**
-   PROBLEM: Experiencing a bottleneck during prefill when the inference process is running with a fairly large context window, and how to overcome it?
-   HYPOTHESIS: Long-context inference can be performed within smaller context windows. However, prefill costs and memory pressure increase non-linearly once a certain threshold is exceeded. This appears to be the behavior of the scheduler regarding CPU threads and token batches during processing.
-   EXPERIMENT: Testing various context lengths (32K, 64K, 128K, 256K) and measuring prefill time, decoding speed, memory usage, and thermal behavior across different quantization levels, while identifying the optimal "sweet spot" for the existing configuration.
-   FAILURE AND NEW ITERATION: With the previous (pre-optimization) configuration, an Out-of-Memory (OOM) error occurred when the context window reached 110K; however, I required a larger 256K context window for research purposes. This necessitated finding an optimal configuration—one that balances prefill and decode speeds reasonably, avoids OOM errors, and maintains KV cache quality (which impacts model memory usage) during inference.
-   ITERATION: Lower the configuration number --n-cpu-moe, reconfigure --threads and --threads-batchs, set the batching process (--batch, and --ubatch) to get optimal results.
-   EVIDENCE (text + 4 stat tiles):
    -   Intro line: Based on the configuration optimization results, I obtained these figures—without compromising model memory quality, reducing the context window size, or drastically altering prefill and decode speeds.
    -   MAX STABLE CONTEXT: 251166
    -   DECODE SPEED AVERAGE: 27.23 tok/s
    -   PROMPT PROCESSING SPEED AVERAGE (Total 256K CONTEXT): Average 314.71 Tok/s
    -   DATA VISUALIZATION: You can see on my chart.
-   RESULT: Long-context inference can be run up to 256K on consumer hardware with the right optimizations. I have not tested beyond that limit, as the model's base specifications—based on data from the developer (Google DeepMind)—indicate a maximum context length of 262,144. Therefore, I did not test it outside the provided official specifications.
-   WHAT I LEARNED: The prefill cost rather than decoding speed is the primary bottleneck in long-context inference. Proper scheduler configuration, model layer placement, expert layer placement, and KV-cache optimization are crucial for the practical implementation of long-context capabilities.
-   PRACTICAL IMPLICATION: For practical applications, a 64K context offers a good balance between capabilities and performance. If you require a context larger than 64K, it is crucial to remember that there is a cost involved. Strategies for context truncation and retrieval should be designed with these practical limitations in mind.

**Configuration Template:**
-   Label: ~/Path build llama.cpp
```
MODEL="path model" (model Gemma 4 26B A4B) \
  --host 0.0.0.0 \
  --port 8080 \
  --n-gpu-layers 99 \
  --threads 4 \
  --threads-batch 4 \
  --n-cpu-moe 23 \
  --moe-cache off \
  --ctx-size 262144 \
  --batch-size 1024 \
  --ubatch-size 1024 \
  --keep 8000 \
  --cache-type-k f16 \
  --cache-type-v f16 \
  --swa-checkpoints 16 \
  --checkpoint-min-step 2048 \
  --embd-normalize 0 \
  --no-kv-unified \
  --kv-offload \
  --jinja \
  --flash-attn on \
  --parallel 1 \
  --cache-ram 8192 \
  --cache-idle-slots \
  --temp 1.1 \
  --top-k 64 \
  --top-p 0.95 \
  --min-p 0 \
  --repeat-penalty 1 \
  --repeat-last-n 0 \
  --alias research \
  --log-verbosity 4
```
-   Explanatory note above list: Do not copy this configuration blindly; it must be adjusted to suit the specific characteristics of your model and hardware. This configuration serves only as a reference, as my choices were based on specific reasons that I will explain below.
-   Numbered explanation (8 points):
    1.  I chose `--n-gpu-layers 99` as a baseline to offload the entire model to the GPU.
    2.  I chose `--threads 4` because testing showed this to be my "sweet spot." This setting configures CPU threads to assist with token generation; the optimal value cannot simply be guessed but must be determined through testing based on your CPU's thread count. I happen to use an i5-11400F (6 cores, 12 threads), and 4 threads worked best for me.
    3.  I chose `--threads-batch 4` because my tests showed that higher values increased prefill latency. This happens because too many CPU threads get involved in the prefill process—a task that should ideally rely more on the GPU to maintain reasonable speeds. However, using fewer threads isn't always faster either; bottlenecks can occur because parts of the model's "expert layers" are offloaded to the CPU. Also, monitor the GPU junction temperature: if prefill feels fast but the junction temperature is high, increase this value.
    4.  I chose `--n-cpu-moe 23` to offload 23 experts to the CPU for optimal performance on my hardware. This value isn't universal; it varies depending on the specific model and its quantization.
    5.  Regarding `batch` and `ubatch`: these values shouldn't be chosen arbitrarily, as they affect compatibility with kernels and the inference engine. Higher numbers don't necessarily yield faster results; in fact, they often lead to anomalies—such as unreasonable GPU junction temperatures or even slower prefill speeds.
    6.  I selected `f16` for KV cache compression to maintain memory consistency for the model. Of course, this reduces the number of model layers that can fit into the GPU, but I chose `f16` because, personally, I find a model losing its context far worse than slow inference speeds.
    7.  I chose `--no-kv-unified` and `--parallel 1` because the model is used only by me in a single inference session. If multiple users were accessing the same model running on your PC, you would need to enable `--kv-unified` and increase the `--parallel` value.
    8.  The remaining configurations I haven't covered are standard settings that you can find in any other post.

**Non-technical summary:**
This experiment showed that a 256K context window is technically achievable even on constrained consumer hardware.
But the bigger finding is that bigger isn't always better.
Pushing the system to its maximum capacity required significant memory and performance trade-offs. In practice, 64K context proved to be a much more balanced configuration for everyday use, offering substantial context capacity without paying the full cost of running at the theoretical maximum.
The takeaway: the goal isn't to make a system as powerful as possible. It's to find the point where available resources, performance, and real-world usefulness are properly balanced.
In other words, this experiment wasn't really about reaching 256K. It was about understanding where "maximum" stops being "practical."

---

## EXPERIMENT 003 CONTENT — "Automation agent workflow for business research needs."

**Sub Tagline:** What does it actually take to optimally use AI as an autonomous research assistant? Is it just the model? As it turns out, no.

**Status Badge:** STABLE

**System Requirements:**
-   MCP Tools: Scrappling
-   Jinja: Custom
-   System Instruction: Deterministic
-   Backend: ROCm Native By The Rock
-   OS: Ubuntu 26.04 LTS
-   Inference Engine: Ollama
-   Frontend UI: OpenWebUI
-   Model: Gemma-4-26B-A4B-Q4_K_XL-GGUF
-   Context: 262144
-   Status: Stable

**Screenshot:** 1 image (Experiment 003.webp)

**Visualization — "Output quality based on system optimization.":**
-   Caption: The percentage is determined based on the degree of coherence between the research output and the research request.
-   Chart type: Step/line chart, y-axis = Success Rate (%) / Output Quality (%)
-   Data (optimization stage → % ): MCP→51%, MCP+RAG→58%, MCP+RAG+KVCM→69%, MCP+RAG+KVCM+LP→71%, MCP+RAG+KVCM+LP+DSP→87%, MCP+RAG+KVCM+LP+DSP+FTD→99%
-   Abbreviation glossary (must render below/beside chart):
    -   MCP: External MCP tools
    -   RAG: Retrieval-Augmented Generation for domain-specific document understanding
    -   KVCM: KV Cache Management
    -   LP: Layer Placement
    -   DSP: Deterministic System Prompt
    -   FTD: Fine Tuning Domain

**Experiment Details:**
-   PROBLEM: My challenge is figuring out how to enable an LLM to understand my research needs, particularly within the context of business requirements research.
-   HYPOTHESIS: When conducting research, we humans require an understanding of the business domain and the necessary tools such as business documentation or prior work experience (for foundational knowledge) and web browsers (as the operational tool). Yet, it goes beyond that; we must be able to interact with website elements, take research notes, and synthesize the findings. The same applies to LLMs; my reasoning leads to the conclusion that AI requires tools, business documents, and an iterative process regarding the specific business being researched.
-   EXPERIMENT: I provide comprehensive business documentation known as RAG in the world of LLMs along with the right tools. I then build an integrated system that combines RAG, external MCP tools (for browsing and data fetching/web scraping used by the LLMs), and a notes function.
-   FAILURE AND NEW ITERATION: Based on the tests I conducted, it turns out that RAG, external MCP tools, and Notes alone are insufficient. Midway through the research process, the model loses its ability to maintain short-term context memory because the prompts being processed are extensive and numerous. Therefore, the entire interconnected system requires optimization: RAG, MCP tools, KV cache management, layer placement, and finally deterministic instructions (system prompts) that the model can easily grasp without stifling its creativity.
    -   Pull-quote (repeated twice in source — likely intentional emphasis, template should render as a highlighted callout): "An additional step can be taken if necessary (domain-specific fine-tuning using a private dataset rather than a public one). If this is done, you will obtain output that precisely matches your requirements."
-   ITERATION: When all these elements come together as a unified whole, the results are highly effective, even if the process takes a bit longer. However, this is a reasonable trade-off, considering that the work is being performed by local LLMs on consumer-grade hardware rather than by massive frontier models with unlimited resources.
-   EVIDENCE (text + 4 stat tiles):
    -   Intro line: You can review the screenshots I provided earlier. They allow you to assess various aspects such as token usage, the number of tool calls made per request, and the number of URLs or websites accessed before the final output is generated. I have also included charts illustrating how the overall system configuration optimizes results; for instance, comparing the model's coherence when using only RAG and tools versus the performance gains achieved by adding KV cache management optimization, and so on.
    -   WORKFLOW COMPLEXITY: 33 tool calling
    -   TOOL SUCCESS RATE: ~99%
    -   TOTAL RESEARCH TIME: ~51 Minutes On 1x Turn
    -   SELF-CORRECTION: Active
-   RESULT: Only after everything is optimized as a unified whole can truly optimal results be realized. The output is spot on — adhering to instructions and incorporating self correction, all achieved with just a single request.
-   WHAT I LEARNED: To achieve optimal results, I shouldn't keep thinking that "this model lacks sophistication" or "it turns out the model isn't that good." That mindset is actually flawed because it relies on a perspective limited to a single environment. In contrast, when looking at current AI applications such as ChatGPT, Claude, Gemini, Qwen Studio, and others — engineers don't focus solely on the "model" itself, they consider the entire operational infrastructure, recognizing that the model is just one of many components requiring optimization.
-   PRACTICAL IMPLICATION: The example I have demonstrated in this experiment represents just one of the many existing types of workflow agents. If you wish to use this approach, ensure you fully understand your environment.

**Configuration Template:** None (this experiment has no CLI config block).

**Non-technical summary:**
This experiment showed that getting an AI to perform complex business research isn't simply a matter of using a more powerful model.
A useful AI system needs more than a model. It needs the right knowledge, the right tools, a way to keep track of what it has learned, and a workflow that allows it to check and correct its own work.
In this experiment, the initial setup was not enough. As the research became longer and more complex, the system began losing coherence. Instead of replacing the model, I optimized the entire system around it.
The result was a workflow capable of performing a complex research task through 33 tool calls, with approximately 99% tool-call success, self-correction enabled, and a complete research process taking roughly 51 minutes from a single request.
The important takeaway is that AI capability doesn't come from the model alone. It comes from how the entire system is designed around it.
A model is the engine. The real capability comes from the machine built around it.

---

## EXPERIMENT 004 CONTENT — "Creating digital employees with the help of open-source LLMs."

**Sub Tagline:** Is it possible to set up customer service, operational admin, ads specialist, data analyst, and finance functions using only workflows and open-source LLMs, given adequate hardware and reasonable resource usage?

**Status Badge:** STABLE (rendered as a large top badge on this page rather than inline with the requirements table)

**System Requirements:**
-   CPU: Intel Core I5 11400F
-   RAM: 16GB DDR4 3200 MT/s
-   GPU: RX 6700 XT
-   OS: Ubuntu 26.04 LTS
-   The Model That Help Me: ORNITH-1.0-25B=A3B-IQ4_NL-GGUF *(note: verbatim as published — likely intended "25B-A3B"; a different/smaller model than exp001's 35B-A3B build)*
-   Inference Engine: Llama.cpp & Ollama
-   Frontend UI: Open Web UI & N8N
-   Model For Parsing: Qwen2.5-1.5B-Instruct-Q8_0-GGUF
-   Status: STABLE
    *(note: no "Context" field on this page, unlike exp001/002/003)*

**Screenshots:** 3 images (exp004-1.webp, exp004-2.webp, exp004-3.webp)

**Visualization — "AI-First vs Deterministic-First":**
-   Caption (Indonesian in source): Perbandingan metodologi yang menentukan keberhasilan eksperimen ini.
-   Type: Comparison visual, not a data chart — a labeled 0% → 100% scale rendered red → green, comparing two methodology states:
    -   Left/lower state label: AI-First
    -   Right/higher state label: Deterministic-First

**Experiment Details:**
-   PROBLEM: Am I capable of creating a system to help me run a service business? I ask because I need to fill a number of vacant positions.
    -   Supporting quotes (4, render as a stacked quote list):
        1.  "An operations administrator responsible for serving customers via WhatsApp, handling the process from the initial chat through to closing the sale."
        2.  "Customer service staff responsible for handling complaints and providing solutions."
        3.  "Data analysis aimed at determining what the market actually needs."
        4.  "An ads specialist capable of interpreting conversion types and linking them to Meta CAPI and Google pixels"
        5.  "A finance system that integrates with payment gateways and can generate invoices when needed."
-   SPECIFICATIONS (extra subsection unique to this experiment — numbered list, 8 items):
    1.  Since this is a service business operating on a home-service model, it is essential to define the service coverage area; based on current operations, this is determined at the sub-district level.
    2.  Since this is a service business with pre-set prices, I need a system capable of interpreting or normalizing customer language.
    3.  In addition to customer language normalization, I also need a system capable of reading and interpreting images or content sent by customers, in order to identify and match them against the price list. Beyond price list issues, I also require a system that can interpret images or videos based on customer complaints.
    4.  I also need data to conduct research for business development and the development of this system itself.
    5.  I need a WhatsApp API that can be integrated with the system I am building.
    6.  I need a database to store all customer data, price lists, complaint types, service areas, and schedule bookings.
    7.  I need a system capable of reading specific customer house numbers to determine whether or not they are detected.
    8.  I need a payment gateway that can be integrated with my system so that every payment can be verified in real-time.
-   HYPOTHESIS: My hypothesis is that I need the necessary tools, such as (numbered list, 6 items):
    1.  Tools that orchestrate all my needs and my choice is n8n, because it has exactly what I require.
    2.  An open-source API for WhatsApp that allows my n8n instance to connect via "webhook" and "HTTP request" nodes; I chose EVOLUTION API because it is an open-source API that provides exactly what I need.
    3.  A database tool for storing all data. I chose PostgreSQL due to its compatibility with the EVOLUTION API and my familiarity with the tool.
    4.  A small LLM model tasked with parsing "keywords," and I chose qwen2.5-1.5B-instruct-Q8_0-GGUF.
    5.  An inference engine tool that can automatically start up when needed and I chose Ollama for its ease of use.
    6.  A payment gateway that can integrate with my n8n setup. I cannot disclose the specific tool yet due to considerations regarding its credibility and legal implications. However, for those following this experiment, you can connect existing payment gateways using the HTTP Request node in n8n.
-   EXPERIMENT: I set out to build this system by leveraging AI to the fullest, using a sufficiently large LLM to bridge the gap regarding technical syntax requirements and integrating AI into every possible node to keep the system streamlined and minimize the node count. However, my reasoning turned out to be completely flawed, and the hundreds of tests I conducted using this approach failed utterly.
-   WHAT FAILED (numbered list, 3 items):
    1.  The AI I installed hallucinates a lot 🤣. Its AI often fails at crucial or critical points, such as cost estimation, coverage validation, and so on.
    2.  The system has become unpredictable; it is difficult for me to determine which parts actually require mitigation, which are erroneous, and which are broken — it is simply too complex.
    3.  The payment gateway — and this is the most crucial point — its AI consistently fails to identify whether the payment has actually been made or not.
-   ITERATION: Reflecting on the failures I've experienced, I've come to realize that the answer isn't always simply "using a more capable AI to build the system and shorten the journey." Those setbacks led me to shift my approach from "AI-first" to "deterministic-first," reserving AI only for instances where it is truly the right fit.
-   EVIDENCE (text + 8 stat tiles):
    -   Intro line: These are the figures I obtained after changing my approach. (You can look at the screenshot I provided above)
    -   HALLUCINATE: 0
    -   TASK NOT COMPLETED: 0
    -   EFFECTIVENESS: 100%
    -   THE SYSTEM HAS BEEN CORRUPTED: Not At All
    -   TOTAL WORKFLOW: 3 separate workflows
    -   TOTAL NODES: 266 Nodes
    -   CAN BE EASILY MITIGATED: Absolutely Yes
    -   IS IT EASY TO DEBUG: 100% YES
-   RESULT: The results I achieved are the product of hundreds of tests and dozens of iterations and methodological adjustments. The outcome is a system that is easy to debug and mitigate; customer inputs not detected by the system are captured in the database to inform upgrades, anomalous inputs are logged for market research, and so on.
-   WHAT I LEARNED: AI is powerful, but its true value lies in using it at the right time, leveraging its strengths, and most importantly knowing how to use it effectively. Through my own experiences with failure, I've learned that AI can be a highly effective tool for running a business like the one I'm currently building without requiring massive resources, provided we understand how to use it and identify the right approaches.
-   PRACTICAL IMPLICATION: This is not a simple task, nor is it something a beginner can do "easily." If you want to build something like this, start by creating a system where the AI parses keywords and generates simple reply templates — from there, you will likely feel challenged and want to take it further.

**Configuration Template:** None (this experiment has no CLI config block).

**Non-technical summary:**
This experiment was not designed merely as a theoretical exercise — it is a real-world implementation that I have actively deployed.
Initially, I employed a method that relied entirely on AI, which resulted in frequent failures — the outcomes were unpredictable, difficult to mitigate, and hard to debug.
I subsequently adopted a different approach, utilizing the AI to write JavaScript code that ensures deterministic output and clear system readability, achieving 100% precision in the journey structure at every node.
I created a total of three separate workflows comprising 266 nodes; only four of these nodes utilize AI for keyword detection, while the remainder consist of standard n8n components such as JavaScript code nodes, HTTP requests, and others.
The system is currently running stably and will undergo continuous upgrades to meet evolving business needs.

---

---
# CONTACT PAGE CONTENT SPECIFICATIONS

## HEADER SECTION
-   Eyebrow Label: CONTACT
-   Title: Have a problem worth exploring?
-   Description: If you have a business problem, technical question, or AI opportunity that needs validation, feel free to reach out.

## CONTACT CHANNELS SECTION
-   EMAIL: ibborarts@gmail.com
-   REDDIT: Full_Director87 → https://www.reddit.com/user/Full_Director87/
-   INSTAGRAM: ybboraa → https://www.instagram.com/ybboraa/
-   (Note: LinkedIn is present in the Footer spec but not shown as a standalone channel on the live Contact page — confirm with bro whether it should be added here for consistency.)

## PRIMARY CTA
-   Button Label: SEND WHATSAPP
-   Link: wa.me/+6282249072873
-   Prefilled message text (as currently published): "Halo Saya, ingin tau lebih detail tentang ai. boleh saya bertanya?"

---

---
# VISUAL REDESIGN SCOPE (NEW — TO GUIDE THE STYLE/VISUAL UPGRADE)

## CONTENT-PRESERVATION RULE
-   All copywriting documented in this PRD (Homepage, Profile, Experiments List, all 4 Experiment Detail pages, Contact) is FINAL and must be carried over verbatim into the redesign.
-   Only visual treatment, layout, spacing, typography, color application, and interaction/animation may change.
-   Verbatim inconsistencies found in the live content (flagged inline above with a "note:") are preserved as-is unless bro explicitly decides to correct them separately from this visual-only PRD.

## REUSABLE COMPONENT INVENTORY (must be (re)designed once, used everywhere)
1.  Navbar (with active-page state + hover state)
2.  Footer (3-column: Brand/Tagline, Navigate, Connect)
3.  Hero block (name/title + tagline + sub-tagline + optional blockquote + CTA(s))
4.  Card — generic (used for: Selected Experiments teaser, Capability, Focus Areas, Principles, Value Proposition — same visual family, different content)
5.  Status Badge (4 variants: Working On It / Exploring / Failed / Stable)
6.  Journey/Timeline item (numbered stage + phase label + description) — Profile page only
7.  Stat/Metric tile (label + value, flexible count per page)
8.  Key–value requirements grid (flexible field count) — Experiment Detail pages
9.  Blockquote / pull-quote (recurring style element across every page)
10. Chart component — must support: bar chart, line chart, and a labeled 0–100% comparison/gradient visual
11. Abbreviation/glossary list (used once, in exp003 — but build as reusable in case future experiments need it)
12. Image gallery (1–N images) — Experiment Detail pages
13. Code block with numbered explanatory list below it (optional per Experiment Detail page)
14. Non-technical summary callout block
15. Primary CTA button (e.g. "VIEW EXPERIMENTS", "SEND WHATSAPP") and secondary/link CTA (e.g. "Back to Experiments")

## RESPONSIVE REQUIREMENTS
-   Desktop, Tablet, Mobile breakpoints must all be defined (exact px breakpoints TBD — default to Tailwind's `md`/`lg` conventions unless bro specifies otherwise).
-   Dynamic components (requirements grid, stat tiles, image gallery, charts) must gracefully reflow at each breakpoint rather than being hardcoded to a fixed column count.

## OPEN ITEMS / TO CONFIRM WITH BRO
-   Exact spacing system, font pairing, and interaction/animation direction for "interactive, modern" feel (not yet specified — needs bro's decision).
-   Whether LinkedIn should be added to the Contact page channel list (currently only in Footer).
-   Whether the 3 currently-unused status states (Working On It / Exploring / Failed) need real content examples for QA, or just visual variants with no live data yet.
-   Acceptance criteria / definition-of-done for the redesign (e.g. visual QA checklist, Lighthouse/perf targets, browser support matrix) — not yet defined.
